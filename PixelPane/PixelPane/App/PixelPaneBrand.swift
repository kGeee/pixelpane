import SwiftUI

/// Brand palette shared by in-app brand marks. Matches the app icon and the
/// marketing site: warm beige crescent on a near-black ink plate.
enum PixelPaneBrand {
    static let beige = Color(.sRGB, red: 0xbc / 255.0, green: 0xa4 / 255.0, blue: 0x83 / 255.0)
    static let ink = Color(.sRGB, red: 0x0b / 255.0, green: 0x0b / 255.0, blue: 0x0c / 255.0)
}
